export const MAX_MEDIA_IMAGES = 100;

export function normalizeAlbumName(value: unknown): string | null {
  if (typeof value !== "string") return null;
  const trimmed = value.trim();
  return trimmed.length > 0 ? trimmed : null;
}
