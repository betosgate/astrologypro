/**
 * Feature flags module.
 *
 * The `LANDING_PAGE_V2` flag that previously lived here was removed on
 * 2026-04-22 — the V2 landing-page architecture is now the only code path.
 *
 * This file is intentionally left empty so nothing accidentally re-adds a
 * `LANDING_PAGE_V2` export. Delete the file entirely in a follow-up cleanup
 * PR once its absence from build output is confirmed.
 */

export {};
