import { RitualActivityClient } from "@/components/admin/ritual-activity-client";

export const dynamic = "force-dynamic";
export const metadata = { title: "Ritual Activity - Admin" };

export default function AdminRitualActivityPage() {
  return <RitualActivityClient />;
}
