// Temporary file created during a v3 unification lint check; safe to delete.
// Named with a leading underscore so Next.js App Router does not treat it as
// a route. Intentionally empty to avoid compilation impact.
export {};
