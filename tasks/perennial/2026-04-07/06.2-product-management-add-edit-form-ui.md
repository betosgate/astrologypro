# Task 06.2: Product Management - Add/Edit Form UI - 2026-04-07

- Status: Planned
- Priority: P1
- Scope: Form UI, Components, Validation, Image Upload
- Task File: `tasks/perennial/2026-04-07/06.2-product-management-add-edit-form-ui.md`

## Objective

Create a high-fidelity "Product Add/Edit" form in the Perennial management section that exactly matches the legacy UI layout. This includes capturing MRP, various pricing tiers, and supporting dual-image uploads.

## UI Fields (Matches Shared Reference Image 1)

- **Name**: Text Input (*)
- **Description**: Textarea (*)
- **MRP**: Number Input (*)
- **Offer Price**: Number Input (*)
- **Preorder Price**: Number Input (*)
- **Priority**: Number/Range Input (*)
- **Select Category**: Dropdown/Select
- **Select Status**: Dropdown (Active/Inactive)
- **Upload Image**: Drag & Drop Zone (Primary image)
- **Details Page Upload Image**: Drag & Drop Zone (Detailed view image)

## Implementation Steps

1.  **Form Component**:
    - Create `ProductAddEditForm.tsx`.
    - Use React Hook Form or Zod for validation.
    - Organize fields into a 2 or 3-column grid to match the original layout.

2.  **Input Components**:
    - Build customized `TextInput`, `NumberInput`, and `Select` components with the signature dark aesthetics from the reference image.

3.  **Image Upload Integration**:
    - Implement file handling for the two upload areas.
    - Show filename/thumbnail after selecting a file.
    - Handle upload progress if necessary.

4.  **Form Actions**:
    - **Submit**: Call the unified `POST/PUT` API.
    - **Cancel**: Navigate back to the product list.
    - **Reset**: Clear all form fields to default values.

5.  **Edit Integration**:
    - If `productId` is passed in the URL (e.g., `/admin/perennial/products/edit/[id]`), fetch details and pre-fill the form.

---

## Done Definition

- All **10+ requested fields** are present in the form UI.
- Required field validation (marked with *) works correctly.
- Image upload zones trigger file selection and show preview.
- "Submit" successfully saves new products to the database.
- "Reset" button clears the form state correctly.
- UI styling follows the dark theme of the Perennial dashboard.
- **Success/Error Messaging**:
    - **Success**: Displays a clear "Product created successfully!" or "Product updated successfully!" notification (Toast) on valid API response.
    - **Error**: Displays an inline error message or toast containing the specific error from the API (e.g., "MRP is required," "Network error") on failure.
