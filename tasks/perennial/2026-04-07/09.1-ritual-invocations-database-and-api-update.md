# Task 09.1: Ritual Database and API Update for Video - 2026-04-07

- Status: Completed (2026-04-08, upstream)
- Completion Notes: Migration `20260407000104_ritual_invocations_add_video_url.sql` adds `video_url TEXT`. Admin + community + playback APIs all updated to read/write the column.
- Priority: P1
- Scope: Database Migration, API modifications
- Task File: `tasks/perennial/2026-04-07/09.1-ritual-invocations-database-and-api-update.md`

## Objective

Update the underlying schema for the rituals module to support video media. The current schema (`ritual_invocations`) only holds text instructions, which prevents the playback logic from functioning as originally intended.

## Implementation Steps

1.  **Database Migration**:
    - Create a new migration file (e.g., `20260408000001_add_ritual_videos.sql`).
    - Execute `ALTER TABLE ritual_invocations ADD COLUMN video_url TEXT;`.
    - (Optional) Add a `video_thumbnail` or `duration_secs` field if deemed necessary for UI polish.

2.  **Admin API Update (`/api/admin/rituals/[id]`)**:
    - Update the `GET`, `POST`, and `PATCH` methods in the Admin API to accept and return the `video_url` field.

3.  **Community API Update (`/api/community/rituals/[id]`)**:
    - Ensure the player compilation endpoint fetches the `video_url` for passing down to the client.
    - Example update in `.select("id, name, description, instructions, priority, video_url")`.

## Done Definition

- Database accepts `video_url` entries.
- API endpoints correctly handle reading and writing the new video field.
