# Task 10.3: Membership Card Legacy Billing Buttons UI - 2026-04-07

- Status: Completed (2026-04-08)
- Completion Notes: `MembershipCard` action row now exposes `+ Add Member` (when family with slots), `Upgrade Plan`, `Update Payment` (Stripe Elements modal), `Subscribed` (opens unsubscribe modal — only when active), and `Manage Billing` (legacy fallback). Dashboard never leaves the page for cancellation or payment-method updates.
- Priority: P2
- Scope: Dashboard UI component overhaul
- Task File: `tasks/perennial/2026-04-07/10.3-membership-card-legacy-billing-buttons.md`

## Objective

Update the dashboard's `<MembershipCard />` component to include the "Subscribed" button precisely according to the legacy Perennial Mandalism dashboard screenshot, acting as the trigger point for the new custom billing flow modals.

## Implementation Steps

1.  **Component Refactor**:
    - Open `src/components/community/membership-card.tsx`.
    - Restructure the inner `<CardContent />` to mirror the legacy grid alignment.
    - Place the "Next Renewal" horizontal slot properly.

2.  **Add Subscribed/Unsubscribe Toggle UI**:
    - Build the dark/grey "Subscribed" pill button matching the screenshot.
    - Insert it to the right of the "Subscription Renewal Date" column.
    - Attach the `onClick` handler that will trigger the modal from **Task 10.1** (Alert!! Unsubscribe).

3.  **Add Update Payment Trigger**:
    - Integrate the "Update Payment Method" action directly into this card section (replacing the generic "Manage Billing" Customer Portal button) so that it securely launches the Stripe Elements modal built in **Task 10.2**.

## Done Definition

- The `<MembershipCard />` visuals match the legacy dashboard styling.
- The "Subscribed" button is present and actively linked to the custom Unsubscribe Alert modal.
- The default Stripe Customer Portal redirect button is successfully replaced.
