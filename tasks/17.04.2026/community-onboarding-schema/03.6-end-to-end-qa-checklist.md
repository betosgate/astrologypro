# 03.6 End-to-End QA Checklist - Community Onboarding Prefill Fix - 2026-04-17

- Status: Planned
- Priority: P0
- Owner: QA / Backend Developer / Codex
- Parent: `00-master-task.md`
- Depends on: `03.1`, `03.2`, `03.3`, `03.4`, `03.5`
- Task File: `tasks/17.04.2026/community-onboarding-schema/03.6-end-to-end-qa-checklist.md`

## Goal

Run the final end-to-end verification before closing the `GET /api/community/onboarding/prefill` 500 error fix.

## Preflight Checklist

- [ ] `03.1` captured the exact original server error.
- [ ] `03.2` verified every selected column against the active database.
- [ ] `03.3` defined safe behavior for a missing or incomplete member profile.
- [ ] `03.4` verified household member prefill behavior.
- [ ] `03.5` implemented the final route contract.

## API Checklist

- [ ] Unauthenticated `GET /api/community/onboarding/prefill` returns `401`.
- [ ] Authenticated user with no member row returns:

  ```json
  {
    "member": null,
    "family_members": []
  }
  ```

- [ ] Authenticated user with a member row returns `200` with `member`.
- [ ] Authenticated user with no household members returns `family_members: []`.
- [ ] Authenticated user with household members receives rows ordered by `created_at`.
- [ ] The endpoint does not return the missing-column schema cache error.

## UI Checklist

- [ ] `/community/onboarding` loads without a visible crash.
- [ ] Existing member data prefills when present.
- [ ] Empty member data falls back to an empty form.
- [ ] Household member rows prefill when present.
- [ ] No infinite loading state remains after prefill succeeds or safely returns empty.

## Schema Checklist

- [ ] `community_members.birth_country` exists or Task 2 remediation is complete.
- [ ] `community_members.occupation` is either present in schema or intentionally removed from the prefill select after product/backend approval.
- [ ] `community_family_members.invite_email` exists if selected.
- [ ] `community_family_members.user_id` exists if selected.
- [ ] Schema cache has been refreshed if any schema change was applied.

## Closeout Notes

Record the following before marking Task 3 complete:

- [ ] Environment tested.
- [ ] Test user used.
- [ ] Original server error and root cause.
- [ ] Schema changes or cache reloads applied.
- [ ] Route code changes made.
- [ ] Final `GET /api/community/onboarding/prefill` status and response body.
- [ ] Any skipped scenarios or follow-up risks.

## Acceptance Criteria

- [ ] `/community/onboarding` loads successfully.
- [ ] Prefill endpoint returns `200` for expected authenticated empty states.
- [ ] Prefill endpoint still returns `401` for unauthenticated requests.
- [ ] Real unexpected failures are logged and return controlled `500`.
- [ ] Handoff notes are complete enough for another developer to audit what changed.
