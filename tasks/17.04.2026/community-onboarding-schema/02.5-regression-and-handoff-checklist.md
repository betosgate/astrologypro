# 02.5 Regression and Handoff Checklist - 2026-04-17

- Status: Planned
- Priority: P0
- Owner: QA / Backend Developer
- Parent: `00-master-task.md`
- Depends on: `02.4-test-onboarding-complete-payload.md`
- Task File: `tasks/17.04.2026/community-onboarding-schema/02.5-regression-and-handoff-checklist.md`

## Goal

Confirm the fix persists data correctly and does not break nearby community onboarding/profile flows.

## Persistence Checks

After a successful onboarding submit, verify the test member row:

```sql
SELECT id, user_id, first_name, last_name, birth_city, birth_country, onboarding_completed
FROM community_members
WHERE user_id = '<TEST_AUTH_USER_ID>';
```

Expected:

```txt
birth_country = Capitol Planning Region
onboarding_completed = true
```

## Regression Checks

- [ ] `/community/onboarding` loads without crashing.
- [ ] `/community/onboarding` can prefill existing member data.
- [ ] `/community` dashboard no longer fails when reading profile completion fields.
- [ ] `/community/profile` or related profile pages still render existing birth location fields.
- [ ] Existing `community_family_members.birth_country` behavior is unchanged.

## Handoff Notes To Record

Document the final state in the ticket or PR:

- Whether migration `20260413000198` was already applied.
- Whether a manual SQL migration was applied.
- Whether `NOTIFY pgrst, 'reload schema';` was run.
- Whether the app server was restarted.
- Test account or environment used for verification.
- Final endpoint response.

## Acceptance Criteria

- [ ] `community_members.birth_country` persists the submitted value.
- [ ] `onboarding_completed` is set to `true` after successful submit.
- [ ] No nearby community page reports the missing-column error.
- [ ] Handoff notes clearly state exactly what changed in the database/environment.
