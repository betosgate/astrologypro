# 02.6 End-to-End QA Checklist - Community Onboarding Submit Schema Fix - 2026-04-17

- Status: Planned
- Priority: P0
- Owner: QA / Backend Developer / Codex
- Parent: `00-master-task.md`
- Depends on: `02.1`, `02.2`, `02.3`, `02.4`, `02.5`
- Task File: `tasks/17.04.2026/community-onboarding-schema/02.6-end-to-end-qa-checklist.md`

## Goal

Run the final end-to-end verification before closing the `POST /api/community/onboarding/complete` `birth_country` schema cache fix.

## Preflight Checklist

- [ ] `02.1` confirms frontend and API both use `birth_country`.
- [ ] `02.2` confirms the active database schema state.
- [ ] `02.3` confirms the migration was applied or schema cache was refreshed.
- [ ] `02.4` confirms the endpoint returns success for a valid payload.
- [ ] `02.5` confirms persistence and nearby page regressions.

## Database Checklist

- [ ] `community_members.birth_country` exists in the active database.
- [ ] Column type is string-compatible: `VARCHAR`, `TEXT`, or equivalent.
- [ ] Column is nullable.
- [ ] Migration `20260413000198_add_birth_country_to_community_members.sql` is applied, or an equivalent environment migration is documented.
- [ ] Supabase/PostgREST schema cache was refreshed with `NOTIFY pgrst, 'reload schema';` if needed.

## API Checklist

- [ ] Authenticated `POST /api/community/onboarding/complete` returns `200`.
- [ ] Response body is:

  ```json
  { "success": true }
  ```

- [ ] The old error does not appear:

  ```txt
  Could not find the 'birth_country' column of 'community_members' in the schema cache
  ```

- [ ] Unauthenticated request still returns `401 Unauthorized`.
- [ ] Invalid payload still returns validation errors instead of silently saving partial onboarding data.

## UI Checklist

- [ ] `/community/onboarding` loads for an incomplete active community member.
- [ ] The birth country field can be filled with:

  ```txt
  Capitol Planning Region
  ```

- [ ] Submitting the onboarding form completes successfully.
- [ ] User is routed to the expected post-onboarding community destination.
- [ ] Refreshing the community area does not send the user back to onboarding after successful completion.

## Persistence Checklist

Run this check for the test user:

```sql
SELECT id, user_id, birth_city, birth_country, onboarding_completed
FROM community_members
WHERE user_id = '<TEST_AUTH_USER_ID>';
```

Expected:

```txt
birth_country = Capitol Planning Region
onboarding_completed = true
```

## Closeout Notes

Record the following before marking the submit schema task complete:

- [ ] Environment tested.
- [ ] Test user used.
- [ ] Whether migration was newly applied or already present.
- [ ] Whether schema cache reload was required.
- [ ] Whether the app server was restarted.
- [ ] Final API response status and body.
- [ ] Any follow-up risks or skipped checks.

## Acceptance Criteria

- [ ] Full onboarding submit flow succeeds from UI.
- [ ] API succeeds with a valid authenticated payload containing `birth_country`.
- [ ] Database stores `Capitol Planning Region`.
- [ ] No submit-route schema cache error remains.
- [ ] Handoff notes are complete enough for another developer to audit what changed.
