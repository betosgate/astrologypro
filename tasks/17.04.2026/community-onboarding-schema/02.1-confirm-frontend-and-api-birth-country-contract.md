# 02.1 Confirm Frontend and API Birth Country Contract - 2026-04-17

- Status: Planned
- Priority: P0
- Owner: Junior Developer / Codex
- Parent: `00-master-task.md`
- Depends on: None
- Task File: `tasks/17.04.2026/community-onboarding-schema/02.1-confirm-frontend-and-api-birth-country-contract.md`

## Goal

Confirm that the onboarding page and API endpoint agree on the `birth_country` field name before changing database state.

## Files To Inspect

| File | What to verify |
|---|---|
| `src/app/community/onboarding/page.tsx` | Submit payload includes `birth_country: form.birthCountry.trim()`. |
| `src/app/api/community/onboarding/complete/route.ts` | Request body reads `birth_country` and writes it into `updatePayload.birth_country`. |
| `src/app/api/community/onboarding/prefill/route.ts` | If present, confirm prefill reads `birth_country` consistently. |
| `src/app/community/page.tsx` | If dashboard profile completion uses this field, confirm it reads `birth_country`. |

## Steps

1. Search the codebase for `birth_country`.
2. Confirm the frontend sends snake_case `birth_country`, not `birthCountry`.
3. Confirm the API destructures `birth_country` from the request body.
4. Confirm the API writes `birth_country` into the `community_members` update payload.
5. Do not change code unless a naming mismatch is found.

## Codex-Friendly Commands

```bash
rg -n "birth_country|birthCountry" src/app/community src/app/api/community
sed -n '450,510p' src/app/community/onboarding/page.tsx
sed -n '90,170p' src/app/api/community/onboarding/complete/route.ts
sed -n '250,330p' src/app/api/community/onboarding/complete/route.ts
```

## Acceptance Criteria

- [ ] Frontend submit payload sends `birth_country`.
- [ ] API request parsing reads `birth_country`.
- [ ] API update payload writes `birth_country` to `community_members`.
- [ ] Any mismatch found is documented or fixed before moving to database work.
