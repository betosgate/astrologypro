# Task 01.1 - Implement Presign Endpoint

Create a new API route to generate signed upload URLs for Supabase Storage.

## Acceptance Criteria
- [ ] New file created at `src/app/api/admin/training/upload/presign/route.ts`.
- [ ] Endpoint validates that the request is from an authenticated admin.
- [ ] Supports `filename`, `filetype`, and `kind` (video/pdf) inputs.
- [ ] Returns a `signedUrl` from Supabase and the final `publicUrl`.
