# Task 01.2 - Refactor upload-video Frontend

Update the frontend upload utility to use the two-step signed URL process.

## Acceptance Criteria
- [ ] `src/lib/training/upload-video.ts` is refactored.
- [ ] It first calls the `/presign` endpoint to get a signed URL.
- [ ] It uses `XMLHttpRequest` with `PUT` to upload the raw file to the signed URL.
- [ ] Progress tracking (percentage) is maintained.
